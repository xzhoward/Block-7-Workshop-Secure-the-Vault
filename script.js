// Compose a message for the user
const message = "You have received this message because you have been chosen to open an important vault. Here is the secret combination:";

// Get three variables using different arithmetic operators
let num1 = 60 - 50;

let num2 = 5 * 8;

let num3 = 78 / 2;

// Display the vault combination when the button is clicked
function openVault() {
    alert(`${message} \n${num1} - ${num2} - ${num3}`);
}
